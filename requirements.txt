fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5
pandas>=2.0
openpyxl>=3.1
pyyaml>=6.0
pytest>=8.0
# T1 (Tooling Addendum): LangGraph orchestration wrapper. Import-guarded —
# if missing, /demo/run-graph falls back to the plain orchestrator.
langgraph>=0.2,<0.5
